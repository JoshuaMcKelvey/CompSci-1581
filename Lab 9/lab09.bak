;; <Your name here>
;; Lab Exercise 9
;; CS 1581 Honors Computer Science I
;; <Semester and year here, e.g. Fall 2012>

(module lab09 racket

  (provide minimum
	   number-of-nodes
	   inorder
	   postorder
	   insert
	   list->bstree)

  (require "bstree.scm") 

  ;; **************************************************************
  ;; Exercise 8.1, p. 217

  (define minimum      
    (lambda (tree)
      "definition of minimum goes here"))


  ;; **************************************************************
  ;; Exercise 8.2, p. 217

  (define number-of-nodes
    (lambda (tree)
      "definition of number-of-nodes goes here"))


  ;; **************************************************************
  ;; Exercise 8.4, p. 220

  (define inorder
    (lambda (tree)
      "definition of inorder goes here"))


  ;; **************************************************************
  ;; Exercise 8.5, p. 220

  (define postorder
    (lambda (tree)
      "definition of postorder goes here"))


  ;; **************************************************************
  ;; Exercise 8.6, p. 220

  (define insert
    (lambda (n tree)   
      "definition of insert goes here"))


  ;; **************************************************************
  ;; Exercise 8.7, p. 220

  (define list->bstree
    (lambda (lst)
      "definition of list->bstree goes here"))

)